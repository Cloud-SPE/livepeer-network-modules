# Fail loud if the active Node version doesn't satisfy `engines.node` in
# package.json. Combined with .nvmrc (pinned major) this catches "I forgot
# to fnm use" before it manifests as a confusing runtime crash.
engine-strict=true
