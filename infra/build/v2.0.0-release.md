# v2.0.0 image release

Built from `tasks/lpm-v2` at commit `e92ce32bdc69`; every binary is
stamped `version=v2.0.0-e92ce32bdc69`. Verified by deleting the local
copy and pulling back by digest.

An earlier set was pushed from `2666f19` on the same day and is
superseded: the estimator fix (lnm-35a) changed orch-coordinator,
service-registry-daemon and the proto contracts, so that set had two
images containing the fix and four that did not. One tag, one commit —
mixed provenance inside a single tag is the confusion these digests exist
to prevent.

## Digests

| Image | Digest |
|---|---|
| `livepeer-capability-broker` | `sha256:3642e4827c42b977cf5f0b2445a54040c5a0a005c2e86852bbd2a0207b003de9` |
| `livepeer-payment-daemon` | `sha256:3d1db2e43c2258de103182cec9b198a3b8d8f48fc59d3c199b6b20d60fc99d39` |
| `livepeer-protocol-daemon` | `sha256:6a07238dab3ae12465ccd25aa0594d733b6c66e21866fcddb3c2e3c282a2e705` |
| `livepeer-service-registry-daemon` | `sha256:c091d0f1a09b43f427e7fb805c2c5342d3be68800cc14e7e1420c0dc73d5dff8` |
| `livepeer-orch-coordinator` | `sha256:a9205adbe27681e506205d3af6d1d0c4635aca49358f854f2418fb0cfd71474c` |
| `livepeer-secure-orch-console` | `sha256:1ff3e0adf86018bc4303eadeecfa20beb515e0eac1f4e88afe91509c14193b50` |
| `livepeer-conformance` | `sha256:65455060875a1dee9d1e3cd7f5b453e1af363bfd9e4d5a2e058e06f149f4fca3` |

All under `tztcloud/`.

Six of these deploy. `livepeer-conformance` does not — it is the suite a
downstream team runs against its own broker, published because
`livepeer-network-protocol/conformance/README.md` already documented
pulling it.

## Three tags were moved, not created

`payment-daemon`, `protocol-daemon` and `service-registry-daemon` already
had a `v2.0.0` from 2026-04-28, published under an older numbering scheme
that ran v1 through v4. Those images still exist; they are simply no
longer reachable by tag. Their digests are in
`v2.0.0-preexisting-digests.txt`, which is what makes this reversible.

Worth knowing when reading the registry: the `1.4.x` line is **newer** in
wall-clock time than the old `v2.x`–`v4.x` tags. Sorting the tag list by
version number reads the history backwards.

## Pinning by digest

Compose renders `${REGISTRY}/livepeer-<name>:${TAG}`, and a digest cannot
be substituted through `TAG` — `repo:@sha256:...` is not a valid
reference. Pinning therefore means editing the `image:` lines to
`repo@sha256:...` rather than setting a variable. The digests above are
the values to use.

Left as-is rather than reworked, because changing every stack's `image:`
line to a digest-shaped variable is a deployment-ergonomics decision, not
a build one.
