# v2.0.0 image release — 2026-08-22

Built from `tasks/lpm-v2` at commit `2666f19f7ca5`; binaries carry
`version=v2.0.0-2666f19f7ca5`. Verified by deleting the local copy and
pulling `livepeer-capability-broker` back by digest.

## Digests

| Image | Digest |
|---|---|
| `livepeer-capability-broker` | `sha256:a45bf830dc3bfbf3922ad4db6ffcf43ae1990152966c0a20d5b7eb7a63df006b` |
| `livepeer-payment-daemon` | `sha256:ab929077d9dec51fe8526ce7d1a7c2d52cedfe9d82fcb5adf7dcf79ac2cdf79d` |
| `livepeer-protocol-daemon` | `sha256:273a6672afdb4d224e340c8c5f37ec8728276862ca19587ecf0ffc909853f4c1` |
| `livepeer-service-registry-daemon` | `sha256:24411de25f2aa790fcc362f02cb1f7a7b0b38ad303412ce1788bab41d18bde99` |
| `livepeer-orch-coordinator` | `sha256:f3ca919bb4131421ff0323a4fe67184bf6976d1f75046665472d59059f8677c7` |
| `livepeer-secure-orch-console` | `sha256:547a040cdda0fece7b66ab0220747931e29e223675ddc7061ce42ce971de396f` |
| `livepeer-conformance` | `sha256:21d4f7ea7f0995501f01818d2e048ac9ab839eb73d7df2ebb1550fade5538be1` |

All under `tztcloud/`.

`livepeer-conformance` is not deployed — it is the suite a downstream team
runs against its own broker, published because
`livepeer-network-protocol/conformance/README.md` already documents
pulling it. It was built from `fc2407f`, one commit later than the other
six, which is the commit that made it pushable.

## Three tags were moved, not created

`payment-daemon`, `protocol-daemon` and `service-registry-daemon` already
had a `v2.0.0` from 2026-04-28, published under an older numbering scheme
that ran v1 through v4. Those images still exist; they are simply no
longer reachable by tag. Their digests are in
`v2.0.0-preexisting-digests.txt`, which is what makes this reversible.

Worth knowing when reading the registry: the `1.4.x` line is **newer** in
wall-clock time than the old `v2.x`–`v4.x` tags. Sorting the tag list by
version number does not give you the history.

## Pinning by digest

Compose renders `${REGISTRY}/livepeer-<name>:${TAG}`, and a digest cannot
be substituted through `TAG` — `repo:@sha256:...` is not a valid
reference. Pinning therefore means editing the `image:` lines to
`repo@sha256:...` rather than setting a variable. The digests above are
the values to use.

Left as-is rather than reworked, because changing every stack's `image:`
line to a digest-shaped variable is a deployment-ergonomics decision, not
a build one — see lnm-uzc.
