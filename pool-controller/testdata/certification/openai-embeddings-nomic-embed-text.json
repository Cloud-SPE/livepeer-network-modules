[
  {
    "name": "ready",
    "type": "readiness",
    "required": true
  },
  {
    "name": "smoke",
    "type": "request",
    "required": true,
    "config": {
      "assert": [
        {
          "min": 1,
          "path": "$.data[0].embedding"
        },
        "$.usage.total_tokens"
      ],
      "body": {
        "input": "ping",
        "model": "{{identity.openai.model}}"
      }
    }
  },
  {
    "name": "usage",
    "type": "usage",
    "required": true,
    "config": {
      "min_units": 1
    }
  }
]
