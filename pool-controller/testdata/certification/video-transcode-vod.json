[
  {
    "name": "ready",
    "type": "readiness",
    "required": true,
    "config": {
      "attempts": 3,
      "interval_ms": 2000
    }
  },
  {
    "name": "smoke",
    "type": "request",
    "required": true,
    "timeout_ms": 60000,
    "config": {
      "body": {
        "output_url": "{{sink_url}}",
        "profiles": [
          {
            "fps": 30,
            "height": 720,
            "name": "720p30",
            "width": 1280
          }
        ],
        "source_url": "{{fixture_url.video/mp4-2s-720p}}"
      },
      "transport": "stream"
    }
  },
  {
    "name": "usage",
    "type": "usage",
    "required": true,
    "config": {
      "min_units": 1
    }
  },
  {
    "name": "latency",
    "type": "latency",
    "required": false,
    "config": {
      "p50_max_ms": 20000,
      "samples": 2
    }
  }
]
