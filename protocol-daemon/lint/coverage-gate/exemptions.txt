# coverage-gate exemptions
#
# One import path per line. Every exemption MUST have a reason on the
# same line after '#'. Do not exempt a package just because writing
# tests is hard. Exemption is a last resort when the code path is
# genuinely impractical to cover (CLI entry-points gated on os.Exit,
# generated code with no logic, etc.).

# cmd entry point: most of run.go is provider construction + flag
# parsing + os.Exit paths driven by signal context cancellation. The
# happy-path provider wiring is exercised by examples/minimal-e2e at the
# integration level. Unit tests cover the testable pure-logic helpers
# (splitCSV, buildLogger, slogAdapter, run with --help / invalid mode /
# dev boot+shutdown). The remaining 40-something percent is dial-real-
# RPC + sign-real-tx code paths exercised end-to-end, not unit-tested.
github.com/Cloud-SPE/livepeer-network-rewrite/protocol-daemon/cmd/livepeer-protocol-daemon  # cmd entry; covered end-to-end by examples/minimal-e2e

# Documentation-only root package — doc.go contains nothing but the
# package-level comment that links out to the README and design-docs.
github.com/Cloud-SPE/livepeer-network-rewrite/protocol-daemon  # doc-only root package

# Generated proto/gRPC code. Stubs land via `make proto` (buf generate);
# they're checked in to keep CI builds working without buf installed.
# Plan 0014 wired up the listener/adapter that consumes these stubs.
github.com/Cloud-SPE/livepeer-network-rewrite/proto-contracts/livepeer/protocol/v1  # generated by buf; consumed via internal/runtime/grpc

# Lint testdata fixtures consumed by no-secrets-in-logs analysers.
github.com/Cloud-SPE/livepeer-network-rewrite/protocol-daemon/lint/no-secrets-in-logs/testdata  # test fixtures

# Example end-to-end demo. Smoke-tested via go run; unit tests would
# duplicate the examples/minimal-e2e/main.go scenario without value.
github.com/Cloud-SPE/livepeer-network-rewrite/protocol-daemon/examples/minimal-e2e  # demo plumbing
