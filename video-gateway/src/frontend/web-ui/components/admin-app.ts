// Operator admin SPA root component. Ported from
// livepeer-network-suite/livepeer-video-gateway/web-ui/src/components/admin-app.ts.
// The actual Lit element wiring lands when Vite + Lit decorators are
// enabled at the frontend sub-workspace (phase 5 wiring).

export const VIDEO_GATEWAY_ADMIN_APP_TAG = "video-gateway-admin";
